{
  "name": "tokimizer",
  "version": "1.0.0",
  "description": "Self-learning Claude Code context optimizer",
  "main": "plugin.json",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": { "node": ">=18.0.0" }
}
