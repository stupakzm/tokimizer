# /tokimizer:status Command Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a `/tokimizer:status` slash command that gives a quick, single-glance view of plugin health, state directory, last flush time, budget, session buffer, and pending ignore candidates — without running the full `/tokimizer:analyze` report.
**Architecture:** Two markdown files only — a new slash command doc at `commands/status.md` and an update to the commands table in `skills/tokimizer/SKILL.md`. No hooks, no JavaScript, no test changes.
**Tech Stack:** Markdown slash command (no code)

---

## File Map

| File | Change |
|------|--------|
| `commands/status.md` | Create — full slash command definition |
| `skills/tokimizer/SKILL.md` | Update — add `/tokimizer:status` row to commands table |

---

### Task 1: Create `commands/status.md`

**File:** `commands/status.md`

- [ ] **Step 1: Create the file with the following exact content**

Create `commands/status.md` in the project root `commands/` directory with this content:

```markdown
---
description: Show a quick status summary — plugin state, last flush, budget, and pending actions
---

# /tokimizer:status

Show a concise status snapshot for Tokimizer: whether the plugin is active, where state is stored, when scores were last flushed, current token budget, session buffer activity, and any pending ignore suggestions.

## Steps

1. Locate the state directory using the same global/local detection logic as session-start:
   - Check `.claude/settings.json` in the project root for `enabledPlugins.tokimizer: true`.
   - If found, the state directory is `.claude/tokimizer/` (local install).
   - Otherwise the state directory is `~/.claude/tokimizer/<project-hash>/` (global install).

2. If `file-map.json` does not exist in the state directory, report:
   ```
   [Tokimizer] Not initialized. Run /tokimizer:reindex to set up this project.
   ```
   Then stop.

3. Read `file-map.json`. Extract:
   - `config.token_budget_pct` — budget percentage (e.g. `35`)
   - `config.context_window` — context window size (e.g. `200000`)
   - `config.project_total_tokens` — total indexed tokens
   - `last_updated` — ISO timestamp of the last score flush
   - Count of keys in the `files` object — number of tracked files

4. Read `session-buffer.json` if it exists. Extract the length of the `accesses` array. If the file does not exist, treat access count as 0 and mark the buffer as empty.

5. Read `suggestions.txt` if it exists. Count the non-empty lines. If the file does not exist or is empty, pending count is 0.

6. Compute derived values:
   - `budget_tokens = floor(config.context_window * config.token_budget_pct / 100)`
   - `time_ago` — human-readable elapsed time since `last_updated` (e.g. "3 hours ago", "2 days ago")

7. Display the formatted status block:

```
=== Tokimizer Status ===

Plugin:   active
State:    ~/.claude/tokimizer/a3f9c2/ (global)
          — or —
State:    .claude/tokimizer/ (local)

Last flush:    2026-04-10 09:12 UTC (3 hours ago)
Files tracked: 847
Budget:        35% of 200,000 tokens → ~70,000 tokens/session

Session buffer: active (12 accesses buffered)
              — or —
Session buffer: empty

Pending ignores: 3 candidates (run /tokimizer:optimize)
              — or —
Pending ignores: none
```

Use the actual values read from the files, not the example values above. Show the "global" or "local" label to match the detected install type. Show the "active (N accesses buffered)" variant when access count > 0, and "empty" when access count is 0 or the file is missing. Show the "N candidates (run /tokimizer:optimize)" variant when pending count > 0, and "none" when pending count is 0.
```

---

### Task 2: Update `skills/tokimizer/SKILL.md`

**File:** `skills/tokimizer/SKILL.md`

- [ ] **Step 1: Add the `/tokimizer:status` row to the commands table**

In `skills/tokimizer/SKILL.md`, locate the commands table:

```markdown
| Command | Purpose |
|---------|---------|
| `/tokimizer:reindex` | Scan full project, set token budget (run once on first use) |
| `/tokimizer:analyze` | Show token usage report and file scores |
| `/tokimizer:optimize` | Review and apply .claudeignore suggestions |
```

Replace it with:

```markdown
| Command | Purpose |
|---------|---------|
| `/tokimizer:reindex` | Scan full project, set token budget (run once on first use) |
| `/tokimizer:analyze` | Show token usage report and file scores |
| `/tokimizer:optimize` | Review and apply .claudeignore suggestions |
| `/tokimizer:status` | Quick status: plugin state, last flush, budget, buffer, pending ignores |
```

---

### Task 3: Commit

- [ ] **Step 1: Stage and commit both files**

```bash
git add commands/status.md skills/tokimizer/SKILL.md
git commit -m "feat(commands): add /tokimizer:status slash command

Adds a quick-status command that reports plugin health, state directory
location, last flush time, current budget, session buffer activity, and
pending ignore candidates in a single compact block — without running
the full /tokimizer:analyze report."
```

---

## Verification Checklist

- [ ] `commands/status.md` exists and contains a valid YAML front matter block with a `description` field
- [ ] The Steps section in `commands/status.md` covers all six steps: locate state dir, missing-file guard, read `file-map.json`, read `session-buffer.json`, read `suggestions.txt`, display formatted output
- [ ] Both conditional variants are documented (global/local, active/empty buffer, candidates/none)
- [ ] `skills/tokimizer/SKILL.md` commands table contains exactly four rows (reindex, analyze, optimize, status)
- [ ] No implementation files (hooks, tests, package.json) were modified
- [ ] Commit contains exactly two changed files: `commands/status.md` and `skills/tokimizer/SKILL.md`
